/** Set enterprise campaign banner on VMware exit solution pages. */
(function () {
    'use strict';
    if (window.ElementoBanner?.setEnterpriseIntent) {
        window.ElementoBanner.setEnterpriseIntent();
    }
})();
