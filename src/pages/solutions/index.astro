---
import MigratedPage from '../../components/MigratedPage.astro';
import { pageByStem } from '../../lib/manifest';

const entry = pageByStem('solutions/index', 'en');
if (!entry) throw new Error('Missing manifest entry: solutions/index');
---

<MigratedPage entry={entry} locale="en" />
